# نظام الخرائط الزراعي - Mapping

تطبيق Flutter متكامل لإدارة الأراضي الزراعية، يتيح رسم وتتبع قطع الأراضي على خرائط Mapbox مع تحليل صحة المحاصيل عبر مؤشر NDVI.

## 📱 الميزات الرئيسية

### رسم الأراضي
- **أشكال جاهزة**: دائرة، مربع، مستطيل، مضلع منتظم (3-12 ضلع)
- **رسم مخصص**: تحديد النقاط واحدة تلو الأخرى
- **رسم حر**: رسم خطوط متواصلة
- **تراجع/إعادة**: دعم كامل للتراجع والإعادة أثناء الرسم
- **تحريك وتحجيم**: سحب الأشكال وتغيير حجمها بالإيماءات

### إدارة الأراضي
- قائمة شاملة بجميع قطع الأراضي
- بحث وتصفية حسب الاسم ونوع المحصول
- عرض تفاصيل كل قطعة (المساحة، الإحداثيات، التاريخ)
- تعديل وحذف القطع المحفوظة
- الانتقال السريع للموقع على الخريطة

### تحليل المحاصيل
- عرض مؤشر NDVI التاريخي
- مقياس صحة المحصول الدائري
- إحصائيات الرطوبة والحالة الصحية
- تكامل مع API للتحليل السحابي

### الخريطة
- خرائط Mapbox عالية الجودة
- عرض الأقمار الصناعية والشوارع
- تحديد الموقع الحالي
- دعم اللغة العربية RTL

## 🛠️ المتطلبات

- Flutter SDK >= 3.4.0
- Dart SDK >= 3.4.0
- حساب Mapbox مع Access Token

## ⚙️ الإعداد

### 1. استنساخ المشروع

```bash
git clone <repository-url>
cd mapping
```

### 2. تثبيت الاعتماديات

```bash
flutter pub get
```

### 3. إعداد Mapbox Token

افتح الملف `lib/config/mapbox_config.dart` وأدخل Access Token الخاص بك:

```dart
static const String accessToken = 'pk.your_mapbox_token_here';
```

للحصول على Token:
1. أنشئ حساب في [Mapbox](https://account.mapbox.com/)
2. انتقل إلى [Access Tokens](https://account.mapbox.com/access-tokens/)
3. أنشئ Token جديد أو استخدم الافتراضي

### 4. إعداد Android

في `android/app/build.gradle` تأكد من:

```gradle
android {
    compileSdkVersion 34
    
    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 34
    }
}
```

### 5. إعداد iOS

في `ios/Runner/Info.plist` أضف:

```xml
<key>NSLocationWhenInUseUsageDescription</key>
<string>نحتاج الوصول لموقعك لتحديد مكانك على الخريطة</string>
<key>NSLocationAlwaysUsageDescription</key>
<string>نحتاج الوصول لموقعك لتحديد مكانك على الخريطة</string>
```

## 🚀 التشغيل

### تشغيل في وضع التطوير

```bash
flutter run
```

### بناء APK للإنتاج

```bash
flutter build apk --release
```

### بناء iOS

```bash
flutter build ios --release
```

## 📁 هيكل المشروع

```
lib/
├── config/              # إعدادات التطبيق
│   └── mapbox_config.dart
├── models/              # نماذج البيانات
│   ├── land_parcel.dart
│   ├── crop_statistics.dart
│   └── ndvi_layer.dart
├── providers/           # إدارة الحالة (Provider)
│   ├── drawing_provider.dart
│   ├── parcels_provider.dart
│   ├── map_state_provider.dart
│   └── location_provider.dart
├── services/            # خدمات التطبيق
│   ├── api_service.dart
│   ├── storage_service.dart
│   └── turf_service.dart
├── views/               # الشاشات الرئيسية
│   ├── map_screen.dart
│   ├── parcels_list_screen.dart
│   └── parcel_detail_screen.dart
├── widgets/             # مكونات UI
│   ├── charts/
│   │   ├── health_gauge.dart
│   │   └── ndvi_chart.dart
│   ├── drawing/
│   │   ├── drawing_toolbar.dart
│   │   └── mode_selector.dart
│   └── map/
│       └── mapbox_view.dart
├── utils/               # أدوات مساعدة
│   ├── colors.dart
│   ├── constants.dart
│   ├── draw_shape.dart
│   └── drawing_mode.dart
├── theme/               # المظهر
│   └── app_theme.dart
└── main.dart            # نقطة الدخول
```

## 📦 الاعتماديات الرئيسية

| المكتبة | الغرض |
|---------|-------|
| `mapbox_maps_flutter` | خرائط Mapbox |
| `provider` | إدارة الحالة |
| `hive_flutter` | تخزين محلي |
| `geolocator` | خدمات الموقع |
| `turf` | حسابات هندسية |
| `fl_chart` | رسوم بيانية |
| `syncfusion_flutter_gauges` | مقاييس دائرية |
| `dio` | طلبات HTTP |

## 🔧 توليد الكود

بعد تعديل Models التي تستخدم Hive:

```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

## 🧪 الاختبارات

```bash
flutter test
```

## 📝 ملاحظات التطوير

### إضافة محصول جديد
1. أضف النوع في قائمة المحاصيل المتاحة
2. حدّث الفلاتر في `ParcelsListScreen`

### تعديل أنماط الخريطة
عدّل الروابط في `lib/config/mapbox_config.dart`

### إضافة لغة جديدة
1. أضف الـ Locale في `main.dart`
2. أضف الترجمات في `lib/utils/constants.dart`

## 🤝 المساهمة

1. Fork المشروع
2. أنشئ Branch للميزة (`git checkout -b feature/amazing-feature`)
3. Commit التغييرات (`git commit -m 'Add amazing feature'`)
4. Push للـ Branch (`git push origin feature/amazing-feature`)
5. افتح Pull Request

## 📄 الترخيص

هذا المشروع خاص ولا يُسمح بإعادة توزيعه بدون إذن.

## 📞 الدعم

للاستفسارات والدعم الفني، تواصل عبر:
- البريد الإلكتروني: [your-email@example.com]

---

**الإصدار الحالي:** 1.0.0  
**آخر تحديث:** نوفمبر 2025
